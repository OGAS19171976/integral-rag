__pycache__/
*.py[cod]
.pytest_cache/
.pytest-tmp/
.pytest-work-*/
.venv/
dist/
build/
*.egg-info/
# 评估实验的中间产物:用 `lev view` 从 eval/report*.json 一条命令即可重新生成,
# 不留进仓库(eval/report*.json 本身是留的 —— 它们是可比对的基线)。
eval/runs/
